#ifndef MATH_H
#define MATH_H

/**
 * Для вычисления основных математических функций
 */

// DEFINITIONS

// METHODS
#define min(a, b) (((a) > (b)) ? (b) : (a))
#define max(a, b) (((a) > (b)) ? (a) : (b))


#endif
